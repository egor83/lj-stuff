Given two LiveJournal usernames, fetches lists of friends and friend-ofs for these users, compares them and shows who these two people both follow and are followed by.

Two usernames you can use as an example are drugoi and tema (they have most friend-ofs in LJ)